# Plan_0323: DocumentDB CPU 100% 쿼리 성능 개선 계획

## 1. 문제 현상

DocumentDB CPU 점유율 100%가 지속됨. `currentOp`으로 확인한 결과:

```
opid: 3716458 | secs_running: 1731 (29분)
ns: finance.session_data
pipeline: [{ $match: { session_id, is_hidden:{$ne:true}, raw_data_id:{$in:[수만건 ID 배열]} }}]
```

`killOp`으로 즉시 해소했으나, 근본 원인 해결이 필요함.

---

## 2. 근본 원인 분석

### 2.1 인덱스 불일치

| 기존 인덱스 | 문제 쿼리 패턴 | 문제점 |
|-------------|---------------|--------|
| `{session_id:1, raw_data_id:1}` | `session_id + is_hidden:{$ne:true} + raw_data_id:{$in:[...]}` | `is_hidden` 필드가 인덱스에 없어 COLLSCAN 발생 |
| `{session_id:1, is_hidden:1}` | `session_id + is_hidden + raw_data_id:{$in:[...]}` | `raw_data_id`가 포함되지 않아 $in 매칭 시 풀스캔 |

**핵심**: 3개 필드(`session_id`, `is_hidden`, `raw_data_id`)를 동시에 사용하는 쿼리가 기존 2개 필드 복합인덱스로 커버되지 않음.

### 2.2 대량 `$in` 배열 문제

| 서비스 | 메서드 | $in 크기 | 위치 |
|--------|--------|----------|------|
| `DataTransformService` | `getOriginalData()` | 키워드 매칭 raw_data_id 전체 (수만건 가능) | 라인 253 |
| `ExportService` | `createRawDataSheetOptimized()` | 10,000건 배치 | 라인 241 |
| `ExportService` | `createRawDataSheetOptimizedWithTracking()` | 10,000건 배치 | 라인 856 |
| `ExportService` | `getAllDataWithDetailCluster()` | 10,000건 배치 | 라인 1013 |
| `ExportService` | `getClusterDetailData()` | 페이지 size만큼 (소량) | 라인 454 |
| `ClusteringService` | `batchFetchSessionData()` | raw_data_id 전체 | 라인 1561 |
| `DetailClusteringService` | `batchFetchSessionData()` | raw_data_id 전체 | 라인 1124 |
| `ClusterStatisticsService` | `updateSessionDataStats()` | 500건 배치 | 라인 373 |

### 2.3 `$limit` 제거 영향 (이전 커밋)

이전 수정에서 `getDistinctValuesWithStatus()`, `getDistinctValues()`, `groupByTwoColumns()`의 `$limit`를 제거함.
→ 고유 값이 매우 많은 컬럼(금액 등)에서 `$group` aggregation이 메모리/CPU를 과다 사용할 수 있음.

---

## 3. 개선 계획

### Phase 1: 인덱스 최적화 (즉시 적용 - DB 직접 실행)

#### 3.1 핵심 복합 인덱스 추가

```js
// ★ 가장 중요: session_id + is_hidden + raw_data_id 3필드 커버링 인덱스
db.session_data.createIndex(
  { "session_id": 1, "is_hidden": 1, "raw_data_id": 1 },
  { name: "session_hidden_rawdata_idx", background: true }
)
```

이 인덱스 하나로 다음 쿼리 패턴을 모두 커버:
- `session_id + is_hidden:{$ne:true} + raw_data_id:{$in:[...]}` (DataTransform, Export, Clustering)
- `session_id + is_hidden:{$ne:true}` (getDistinctValues, groupByTwoColumns)
- `session_id + is_hidden:true` (getDistinctValuesWithStatus hidden 파이프라인)

#### 3.2 기존 중복 인덱스 정리 (선택)

추가 후 기존 인덱스와 비교하여 중복되는 인덱스 정리 검토:

| 기존 인덱스 | 새 인덱스와 관계 | 조치 |
|-------------|-----------------|------|
| `session_hidden_idx {session_id:1, is_hidden:1}` | 새 인덱스의 prefix로 커버됨 | 삭제 가능 (Phase 1 이후 모니터링 후 판단) |
| `session_rawdata_idx {session_id:1, raw_data_id:1}` | is_hidden 없이 사용하는 쿼리용으로 유지 | 유지 (updateMulti 등에서 사용) |

#### 3.3 MongoIndexInitializer 코드 반영

`MongoIndexInitializer.java`에 새 인덱스를 추가하여 앱 재시작 시에도 자동 생성되도록 함.

```java
// session_data: 3필드 커버링 인덱스 (쿼리 성능 핵심)
createIndex("session_data", "session_hidden_rawdata_idx",
    new Index().on("session_id", Sort.Direction.ASC)
               .on("is_hidden", Sort.Direction.ASC)
               .on("raw_data_id", Sort.Direction.ASC));
```

---

### Phase 2: 쿼리 패턴 개선 (코드 수정)

#### 2.1 DataTransformService.getOriginalData() - $in 크기 제한

**문제**: 키워드 필터 시 `process_view_data`에서 매칭되는 `raw_data_id` 전체를 `$in`에 넣음. 수만건 이상일 때 CPU 폭발.

**개선**: 2단계 쿼리 대신 aggregation `$lookup` 또는 **$in 배치 분할 + count 최적화** 적용

```java
// Before: raw_data_id 전체를 $in에 넣음
criteria = criteria.and("raw_data_id").in(filteredRawDataIds); // 수만건

// After: 배치 분할 (5,000건 단위) + 페이징은 별도 처리
private static final int IN_BATCH_SIZE = 5_000;
```

#### 2.2 ExportService 배치 쿼리 - 배치 크기 축소

**문제**: 10,000건 단위 `$in` 배치가 여전히 과대

**개선**: 배치 크기를 5,000건으로 축소하고, 쿼리 타임아웃 설정 추가

```java
// Before
int BATCH_SIZE = 10_000;

// After
int BATCH_SIZE = 5_000;
batchQuery.maxTimeMsec(30_000); // 30초 타임아웃
```

#### 2.3 $limit 제거 후 안전장치 추가

**문제**: `getDistinctValuesWithStatus()` 등에서 `$limit` 제거 후 고유값이 수만개인 컬럼에서 과부하 가능

**개선**: `$limit`을 완전히 제거하지 않고, 충분히 큰 안전 한도(50,000) 설정 + 경고 로그

```java
// 안전 한도: 50,000 이상이면 경고 로그 + 제한
private static final int DISTINCT_SAFE_LIMIT = 50_000;

visiblePipeline.add(new Document("$limit", DISTINCT_SAFE_LIMIT));
```

#### 2.4 MongoDB 쿼리 타임아웃 전역 설정

**목적**: 29분처럼 장시간 실행되는 쿼리를 사전에 차단

```java
// MongoTemplate 커스텀 설정 또는 개별 쿼리에 maxTimeMS 적용
query.maxTimeMsec(60_000); // 60초 타임아웃
```

---

### Phase 3: 모니터링/방어 (운영 안정화)

#### 3.1 DocumentDB 슬로우 쿼리 프로파일러 상시 활성화

```js
db.setProfilingLevel(1, { slowms: 3000 }) // 3초 이상 쿼리 기록
```

#### 3.2 Spring Boot 쿼리 로깅 강화

`application.yml`에서 MongoDB 쿼리 실행 시간 로깅:

```yaml
logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG
```

---

## 4. 수정 대상 파일 요약

| 파일 | 변경 내용 | Phase |
|------|----------|-------|
| `MongoIndexInitializer.java` | `session_hidden_rawdata_idx` 인덱스 추가 | 1 |
| `SessionDataService.java` | `$limit` 안전 한도(50,000) 재적용, 쿼리 타임아웃 | 2 |
| `DataTransformService.java` | `getOriginalData()` $in 배치 분할 + count 최적화 | 2 |
| `ExportService.java` | 배치 크기 10,000→5,000, 쿼리 타임아웃 추가 | 2 |
| `ClusteringService.java` | `batchFetchSessionData()` 배치 분할 + 타임아웃 | 2 |
| `DetailClusteringService.java` | `batchFetchSessionData()` 배치 분할 + 타임아웃 | 2 |
| `ClusterStatisticsService.java` | 쿼리 타임아웃 추가 (배치 500건이라 크기는 적절) | 2 |

---

## 5. 적용 현황

- [x] Phase 1-3.1: DB에 `session_hidden_rawdata_idx` 인덱스 직접 생성 완료
- [x] Phase 1-3.3: `MongoIndexInitializer.java` 코드 반영 완료
- [x] Phase 2-2.1: `DataTransformService` $lookup + 배치 분할 적용 완료
- [x] Phase 2-2.2: `ExportService` 배치 크기 5,000 + 타임아웃 300초 적용 완료
- [x] Phase 2-2.3: `SessionDataService` $limit 50,000 안전 한도 + 타임아웃 300초 적용 완료
- [x] Phase 2-2.4: 전체 7개 서비스 쿼리 타임아웃 300초(5분) 적용 완료
- [ ] Phase 3: 모니터링 설정 (배포 후 수행)

## 6. 실행 순서

```
[Phase 1] DB 인덱스 직접 생성 (즉시)
    │
    ├── 1. DocumentDB에 session_hidden_rawdata_idx 생성
    │      → background:true로 서비스 무중단
    │
    └── 2. MongoIndexInitializer.java에 코드 반영
           → 재배포 시 자동 생성 보장

[Phase 2] 코드 수정 (개발 → 테스트 → 배포)
    │
    ├── 1. 전역 쿼리 타임아웃 60초 설정
    ├── 2. DataTransformService $in 배치 분할
    ├── 3. ExportService 배치 크기 축소
    ├── 4. SessionDataService $limit 안전 한도 재적용
    └── 5. Clustering/DetailClustering 배치 분할

[Phase 3] 모니터링 설정 (배포 후)
    │
    ├── 1. 슬로우 쿼리 프로파일러 활성화
    └── 2. 배포 후 CPU 모니터링 (CloudWatch)
```

---

## 6. 예상 효과

| 항목 | Before | After |
|------|--------|-------|
| 3필드 쿼리 인덱스 | COLLSCAN (전체 스캔) | INDEX SCAN |
| $in 최대 크기 | 10,000~무제한 | 5,000 (배치 분할) |
| 쿼리 최대 실행 시간 | 무제한 (29분 실행) | 60초 (maxTimeMS) |
| $group distinct 결과 | 무제한 | 50,000건 안전 한도 |
| CPU 100% 재발 확률 | 높음 | 매우 낮음 |
